#include "Static.h"
#include "Rooms.h"

namespace global
{
    RoomManager manager;
    float SCALE = 0.15f;
    int ScreenWidth = 900;
    int ScreenHeight = 600;
    bool closed = false;
    bool vsyncEnabled = false;
}